import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-ith-firebase',
  webDir: 'build',
  bundledWebRuntime: false,

};

export default config;


// https://capacitorjs.com/telemetry